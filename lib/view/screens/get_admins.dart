import 'package:flutter/material.dart';
import 'package:get/get.dart';

class GetAdmin extends StatelessWidget {
  const GetAdmin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(),
    );
  }
}
